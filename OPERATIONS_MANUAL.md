# ChouChou Production Operations Manual

最終更新: 2026-07-20
対象Version: 1.0.0
対象: Public Site / Admin / Cast Portal / CRM / Reservation / Sales / Payroll / Analytics / Dashboard

## 1. 運用原則

- 日常操作は必要最小権限で行い、ownerアカウントを通常業務に使わない。
- 売上・給与・締め・ユーザー権限の変更は、対象日・対象者・理由を確認してから実行する。
- 本番障害中に複数人が同じデータを同時修正しない。対応責任者を1名決める。
- 削除より非公開・inactiveを優先する。
- 画面上の成功通知だけで完了とせず、一覧・公開画面・監査ログで反映を確認する。
- 復元、締め解除、権限変更はowner承認と操作記録を必須とする。

## 2. ロール別運用

| ロール | 主な用途 | 禁止・制限 |
|---|---|---|
| owner | 全体設定、ユーザー、給与、締め解除、復旧 | 共有アカウント化しない |
| manager | キャスト、シフト、予約、来店、売上 | 給与設定、ユーザー権限変更不可 |
| staff | 予約受付、来店受付、NEWS | 売上、給与、ユーザー管理不可 |
| cast | 自分のCast Portal | 他キャスト・管理画面不可 |

退職・異動時は当日中に`users.status=inactive`相当の管理操作を行い、共有端末からログアウトする。

## 3. 営業開始前チェック

- [ ] Public Site、Admin、Cast Portalへアクセスできる
- [ ] 本日の出勤人数とシフトを確認した
- [ ] 今日・明日の予約一覧を確認した
- [ ] 席状態が前営業日のまま「使用中」になっていない
- [ ] Dashboardの売上・予約・出勤カードに取得エラーがない
- [ ] Functions / Firestore / Storageの障害情報がない
- [ ] 直近バックアップの完了日時を確認した

## 4. 新規キャスト登録

推奨担当: manager以上

1. Adminのキャスト管理から新規追加を開く。
2. 名前、公開プロフィール、年齢・身長等の必要情報を入力する。
3. 公開状態、NEW、おすすめ、新人、ランキング設定を確認する。
4. 画像を登録し、代替テキストに利用できる名前が保存されていることを確認する。
5. 保存後、キャスト一覧と詳細プレビューを確認する。
6. 公開する場合はPublic Siteの一覧・詳細・予約導線をPCとスマホで確認する。
7. Cast Portalを利用する場合はownerがユーザーを作成し、対象castと紐付ける。

注意:

- 同名キャストが存在する場合もIDで管理されるため、誤ったcastとのAuth紐付けに注意する。
- 非公開で入力を完成させてから公開する。
- 削除前に予約、売上、給与、担当顧客との関連を確認する。

## 5. キャスト画像アップロード

1. JPEG / PNG / WebP等、画面が許可する画像を選択する。
2. 正しいキャストの編集画面であることを確認する。
3. アップロード完了通知とプレビューを確認する。
4. Public Siteの一覧・詳細でトリミング、縦横比、表示速度を確認する。
5. スマホ幅でも顔や主要部分が欠けていないことを確認する。

失敗時:

- 再送前に通信、ファイル形式、容量、ログインroleを確認する。
- 同じ画像を連続送信しない。Storageに重複objectができていないか確認する。
- 画像URLだけがFirestoreへ保存され、objectが存在しない状態になっていないか確認する。

## 6. 出勤登録

推奨担当: manager

1. 営業日とキャストを選択する。
2. 出勤時刻、退勤時刻またはLASTを入力する。
3. 重複シフトと日付跨ぎを確認する。
4. 保存後、本日の出勤一覧とDashboard人数を確認する。
5. Public SiteのToday's Cast表示、残り時間、予約ボタンを確認する。

修正時はPublic SiteのRealtime反映を確認し、古い画面だけを見て二重登録しない。

## 7. WEB予約

1. Public Siteの予約フォームから必須項目を入力する。
2. 指名予約ではキャスト名が自動設定されていることを確認する。
3. 送信成功画面を確認する。
4. Admin予約管理に新規予約が表示されることを確認する。
5. 名前・電話番号・LINEによる既存顧客照合結果を確認する。
6. 新規顧客候補の場合は内容を確認してから作成・紐付けする。
7. 予約日時、人数、指名、受付経路を確認し、確認済みへ更新する。

二重送信が疑われる場合は、削除せず時刻・電話・人数で照合して片方をキャンセル扱いにする。

## 8. LINE予約

1. キャスト一覧または詳細のLINE予約ボタンを押す。
2. `[LINEアイコン] LINE予約`が崩れず、対象キャスト名を含む文面が生成されることを確認する。
3. 希望日時・人数・質問欄が表示されることを確認する。
4. LINEで受けた予約をAdminへ登録し、受付経路をLINEにする。
5. 顧客のLINE IDまたは識別情報を、本人同意と店舗方針に沿って記録する。

LINE起動だけではFirestore予約は作成されない。受信後のAdmin登録完了を必ず確認する。

## 9. 来店受付・席管理

1. 対象予約と顧客を確認し、ステータスを受付へ更新する。
2. 人数、指名キャスト、担当キャスト、席タイプを確認する。
3. 利用可能な席へ着席させ、席状態が使用中になることを確認する。
4. 席移動時は移動先の空席状態を確認してから実行する。
5. 延長時は予約/visitの状態と時刻を更新する。
6. キャンセル・無断キャンセルは理由をメモし、通常削除しない。

予約→受付→着席→延長→会計のタイムラインが顧客・来店履歴へ反映されることを確認する。

## 10. 売上入力

推奨担当: manager以上

1. 来店履歴を選択し、顧客・予約・担当キャストが一致することを確認する。
2. セット、飲み放題、延長、ドリンク、ボトル、シャンパン、フードを入力する。
3. 本指名、場内、同伴の件数と対象キャストを確認する。
4. 割引、サービス料、消費税を確認する。
5. 現金、カード、QR、売掛の合計が請求総額と一致することを確認する。
6. 保存後、売上一覧、顧客累計、Dashboard速報へ反映されたことを確認する。
7. 会計済み・完了へ状態を進め、席が解放されることを確認する。

同じ来店に複数売上を作らない。修正時は理由を残し、`businessAuditLogs`を確認する。

## 11. 給与計算

推奨担当: owner

1. 対象月、キャスト、勤務日数・時間を確認する。
2. 時給、各種バック、歩合率が当月の規定と一致することを確認する。
3. 本指名、場内、同伴、ドリンク、ボトル、シャンパンの集計を売上と照合する。
4. 皆勤、特別手当、罰金、前借、源泉、その他控除を入力する。
5. 総支給、控除、差引支給を二者確認する。
6. 保存後、給与明細と監査ログを確認する。

設定変更は過去月へ意図せず影響しないか確認する。給与確定前に月締め状態を確認する。

## 12. 締め処理

推奨担当: owner。営業日締めは権限範囲内でmanagerが実施可能でも、解除はownerのみ。

### 日締め

1. 未会計・処理中予約・使用中席がないことを確認する。
2. 現金、カード、QR、売掛を実額と照合する。
3. 当日売上・客数・客単価を確認する。
4. 日締めを実行し、以後の売上編集が拒否されることを確認する。

### 月締め

1. 対象月の日締め、売上、給与、未処理売掛を確認する。
2. 集計と給与明細を二者確認する。
3. 月締めを実行し、売上・給与編集が拒否されることを確認する。

締め解除はownerが理由を記録し、修正後に再締めする。解除状態を放置しない。

## 13. 監査ログ確認

毎営業日終了後、次を確認する。

- `businessAuditLogs`: 売上修正、給与変更、commission変更、締め、締め解除
- `auditLogs`: ユーザー作成、role・status変更、キャストAuth紐付け
- 操作者UID、role、対象ID、日時、理由が追跡できる
- owner以外による禁止操作や短時間の大量更新がない
- Functions Error LogとRules拒否が急増していない

監査ログは通常業務で削除・修正しない。

## 14. バックアップ確認

毎日:

- [ ] 最新Firestore ExportまたはScheduled Backupの成功時刻
- [ ] Storage複製の成功と主要prefix
- [ ] Auth Exportの保管世代（実施日運用の場合）
- [ ] manifestのproject ID、commit SHA、件数
- [ ] バックアップbucketへの一般公開がない

毎月:

- [ ] 保持期間と容量・費用
- [ ] Storage Soft Delete設定
- [ ] Secret / Auth provider / index / TTL台帳
- [ ] 復元対象を一覧できる

四半期ごとに、[BACKUP_PLAN.md](./BACKUP_PLAN.md)に従い隔離環境で復元訓練する。

## 15. 障害発生時の初動

### 0〜15分

1. 発生時刻、操作、対象画面、ユーザー、エラー文、スクリーンショットを記録する。
2. データ破損が進行する場合は更新操作を停止する。
3. ownerを責任者として招集し、複数人の独立修正を止める。
4. Firebase Status、Functions logs、Browser Console、Network、Rules拒否を確認する。

### 切り分け

| 症状 | 最初の確認 |
|---|---|
| 全ページ表示不可 | Hosting deploy/status、DNS、404/500 |
| データだけ表示不可 | Firestore到達性、Rules、Auth status/role |
| 画像だけ表示不可 | Storage object、download URL、Storage Rules |
| 管理操作だけ不可 | Route Guard、users role/status、Functions logs |
| ユーザー管理不可 | Callable Functions、Admin SDK、Auth、App Check |
| 売上・給与だけ不可 | 締め状態、権限、Transaction、監査ログ |

### 禁止事項

- 原因不明のまま再deployを繰り返さない。
- 本番コレクションを一括削除しない。
- Rulesを`allow read, write: if true`へ変更しない。
- 締めを解除したまま放置しない。
- Secret、Auth hash、顧客情報をチャットや公開チケットへ貼らない。

復元が必要なら[BACKUP_PLAN.md](./BACKUP_PLAN.md)へ移行する。

## 16. 営業終了チェック

- [ ] 全予約が完了・キャンセル・無断キャンセルのいずれか
- [ ] 使用中・清掃中の席状態を確認した
- [ ] 全来店に売上または未会計理由がある
- [ ] 決済別合計と実額が一致する
- [ ] 日締めが完了した
- [ ] 給与へ必要な指名・同伴・ドリンク情報が揃っている
- [ ] 監査ログに不審な操作がない
- [ ] バックアップ成功を確認した
- [ ] 未解決事項を翌日の担当者へ引き継いだ

## 17. デプロイ後スモークテスト

1. Public Siteのトップ、キャスト一覧、詳細、予約導線を確認する。
2. ownerでAdminへログインしDashboardとユーザー管理を確認する。
3. managerで予約・来店・売上を確認し、owner専用設定が拒否されることを確認する。
4. staffで予約・NEWSが利用でき、売上・給与が拒否されることを確認する。
5. castでCast Portalと本人データだけが表示されることを確認する。
6. テスト予約を受付→着席→会計→完了まで進め、テストデータと明示して後処理する。
7. Console Error、Functions Error Log、404、Rules拒否の異常増加を確認する。
8. Git commit SHA、deploy時刻、担当者、結果を運用記録へ残す。
